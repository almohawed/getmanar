# 📊 ربط التقارير بالبيانات الحقيقية من Firestore

## 🎯 الهدف
تحويل جميع الأرقام في التقارير الثلاثة من أرقام ثابتة (mock data) إلى أرقام حقيقية محسوبة من بيانات المدرسة في Firestore.

## 📋 التقارير المطلوب تحديثها

### 1️⃣ تقرير نسب النجاح
**البيانات المطلوبة:**
- نسبة النجاح الإجمالية
- نسبة التميز (الطلاب الممتازون)
- نسبة التحسن (مقارنة بالفصل السابق)
- توزيع مستويات الأداء:
  - ممتاز (90-100)
  - جيد جداً (80-89)
  - جيد (70-79)
  - مقبول (60-69)
  - ضعيف (أقل من 60)
- نسب النجاح حسب الفصول

**المصدر:**
- `students` collection → حقل `excellenceScore`
- `classrooms` collection → لتجميع البيانات حسب الفصل

### 2️⃣ تقرير الفجوات التعليمية
**البيانات المطلوبة:**
- عدد الفجوات المكتشفة
- عدد الفجوات الحرجة
- عدد الفجوات التي تم معالجتها
- قائمة الفجوات حسب:
  - المادة والصف
  - الوصف
  - عدد الطلاب المتأثرين
  - الأولوية (حرجة، متوسطة، منخفضة)

**المصدر:**
- `students` collection → حقل `excellenceScore` (الطلاب الضعفاء)
- `classrooms` collection → لتحديد الصف
- تحليل الدرجات لتحديد المواد الضعيفة

### 3️⃣ تقرير أداء المعلمين
**البيانات المطلوبة:**
- إجمالي عدد المعلمين
- متوسط التميز (من درجات طلابهم)
- متوسط الحصص الأسبوعية
- لكل معلم:
  - الاسم
  - الإيميل (بعد التنظيف)
  - متوسط تميز طلابه
  - عدد الحصص الحالية / الحد الأقصى
  - نسبة الحمل

**المصدر:**
- `users` collection (role = teacher)
- `students` collection → لحساب متوسط التميز
- `schedules` collection → لحساب عدد الحصص

## 🔧 الخطة التنفيذية

### المرحلة 1: إنشاء خدمات الحسابات

#### 1. خدمة حسابات نسب النجاح
```dart
class SuccessRatesService {
  // حساب نسبة النجاح الإجمالية
  Future<double> calculateOverallSuccessRate(String schoolId);
  
  // حساب نسبة التميز
  Future<double> calculateExcellenceRate(String schoolId);
  
  // حساب نسبة التحسن
  Future<double> calculateImprovementRate(String schoolId);
  
  // توزيع مستويات الأداء
  Future<Map<String, double>> calculatePerformanceLevels(String schoolId);
  
  // نسب النجاح حسب الفصول
  Future<Map<String, double>> calculateSuccessRatesByClass(String schoolId);
}
```

#### 2. خدمة حسابات الفجوات التعليمية
```dart
class LearningGapsService {
  // اكتشاف الفجوات
  Future<List<LearningGap>> detectGaps(String schoolId);
  
  // تصنيف الفجوات حسب الأولوية
  Future<Map<String, int>> classifyGapsByPriority(String schoolId);
  
  // حساب عدد الطلاب المتأثرين
  Future<int> countAffectedStudents(String schoolId, String subject);
}
```

#### 3. خدمة حسابات أداء المعلمين
```dart
class TeacherPerformanceService {
  // حساب متوسط تميز طلاب المعلم
  Future<double> calculateTeacherExcellence(String teacherId);
  
  // حساب عدد الحصص
  Future<int> calculateTeacherLoad(String teacherId);
  
  // حساب إحصائيات جميع المعلمين
  Future<List<TeacherStats>> calculateAllTeachersStats(String schoolId);
}
```

### المرحلة 2: تحديث التقارير

#### تقرير نسب النجاح
**الملف:** `lib/src/features/academic/presentation/success_rates_report_screen.dart`

**التغييرات:**
1. استبدال الأرقام الثابتة بحسابات حقيقية
2. إضافة `FutureBuilder` لتحميل البيانات
3. عرض مؤشر تحميل أثناء الحساب
4. تحديث PDF ليستخدم البيانات الحقيقية

#### تقرير الفجوات التعليمية
**الملف:** `lib/src/features/academic/presentation/learning_gaps_report_screen.dart`

**التغييرات:**
1. حساب الفجوات من بيانات الطلاب
2. تصنيف الفجوات حسب الأولوية
3. تحديد المواد الضعيفة
4. تحديث PDF بالبيانات الحقيقية

#### تقرير أداء المعلمين
**الملف:** `lib/src/features/academic/presentation/teacher_performance_report_tab.dart`

**التغييرات:**
1. حساب متوسط التميز من طلاب كل معلم
2. حساب عدد الحصص الفعلي
3. ترتيب المعلمين حسب الأداء
4. تحديث PDF بالبيانات الحقيقية

## 📊 نموذج البيانات

### Student Model (موجود)
```dart
class User {
  String id;
  String name;
  String email;
  UserRole role;
  String? schoolId;
  List<String>? assignedClassIds;
  int excellenceScore; // 0-100
  int? maxWeeklyClasses;
}
```

### Classroom Model (موجود)
```dart
class Classroom {
  String id;
  String name;
  String schoolId;
  List<String> studentIds;
  String? teacherId;
}
```

### LearningGap Model (جديد)
```dart
class LearningGap {
  String id;
  String schoolId;
  String subject;
  String className;
  String description;
  int affectedStudents;
  GapPriority priority; // critical, medium, low
  bool isResolved;
  DateTime discoveredAt;
}

enum GapPriority {
  critical,  // حرجة
  medium,    // متوسطة
  low,       // منخفضة
}
```

## 🔢 معادلات الحساب

### 1. نسبة النجاح
```dart
successRate = (studentsWithScore >= 60 / totalStudents) * 100
```

### 2. نسبة التميز
```dart
excellenceRate = (studentsWithScore >= 90 / totalStudents) * 100
```

### 3. نسبة التحسن
```dart
// يحتاج إلى تخزين البيانات التاريخية
improvementRate = ((currentAvg - previousAvg) / previousAvg) * 100
```

### 4. توزيع المستويات
```dart
excellent = studentsWithScore >= 90
veryGood = studentsWithScore >= 80 && < 90
good = studentsWithScore >= 70 && < 80
acceptable = studentsWithScore >= 60 && < 70
weak = studentsWithScore < 60
```

### 5. اكتشاف الفجوات
```dart
// فجوة حرجة: أكثر من 30% من الطلاب ضعفاء في مادة معينة
if (weakStudentsInSubject / totalStudents > 0.3) {
  priority = GapPriority.critical;
}
```

### 6. متوسط تميز المعلم
```dart
teacherExcellence = sum(studentsScores) / studentsCount
```

## 🚀 خطوات التنفيذ

### الخطوة 1: إنشاء الخدمات
1. إنشاء `lib/src/features/academic/services/success_rates_service.dart`
2. إنشاء `lib/src/features/academic/services/learning_gaps_service.dart`
3. إنشاء `lib/src/features/academic/services/teacher_performance_service.dart`

### الخطوة 2: إنشاء Models
1. إنشاء `lib/src/features/academic/domain/learning_gap.dart`
2. إنشاء `lib/src/features/academic/domain/performance_stats.dart`

### الخطوة 3: تحديث Providers
1. إنشاء `successRatesProvider`
2. إنشاء `learningGapsProvider`
3. إنشاء `teacherPerformanceProvider`

### الخطوة 4: تحديث UI
1. تحديث `success_rates_report_screen.dart`
2. تحديث `learning_gaps_report_screen.dart`
3. تحديث `teacher_performance_report_tab.dart`

### الخطوة 5: تحديث PDF
1. تمرير البيانات الحقيقية إلى دالة `_generatePDF`
2. استخدام البيانات في التقرير بدلاً من الأرقام الثابتة

## ⚠️ ملاحظات مهمة

### 1. الأداء
- استخدام `StreamProvider` للتحديث التلقائي
- تخزين مؤقت للحسابات الثقيلة
- حساب البيانات في الخلفية

### 2. معالجة الأخطاء
- عرض رسالة خطأ واضحة
- توفير بيانات افتراضية في حالة الفشل
- تسجيل الأخطاء للمراجعة

### 3. البيانات الناقصة
- التعامل مع المدارس الجديدة (بدون بيانات)
- عرض رسالة "لا توجد بيانات كافية"
- اقتراح إدخال بيانات الطلاب

### 4. الأمان
- التحقق من صلاحيات المستخدم
- عرض بيانات المدرسة فقط
- حماية البيانات الحساسة

## 📝 مثال على التنفيذ

### مثال: حساب نسبة النجاح
```dart
class SuccessRatesService {
  final FirebaseFirestore _firestore = FirebaseFirestore.instance;
  
  Future<double> calculateOverallSuccessRate(String schoolId) async {
    try {
      // جلب جميع الطلاب في المدرسة
      final studentsSnapshot = await _firestore
          .collection('users')
          .where('schoolId', isEqualTo: schoolId)
          .where('role', isEqualTo: 'student')
          .get();
      
      if (studentsSnapshot.docs.isEmpty) {
        return 0.0;
      }
      
      // حساب عدد الطلاب الناجحين (درجة >= 60)
      int successfulStudents = 0;
      for (var doc in studentsSnapshot.docs) {
        final score = doc.data()['excellenceScore'] as int? ?? 0;
        if (score >= 60) {
          successfulStudents++;
        }
      }
      
      // حساب النسبة المئوية
      final successRate = (successfulStudents / studentsSnapshot.docs.length) * 100;
      return successRate;
    } catch (e) {
      print('Error calculating success rate: $e');
      return 0.0;
    }
  }
}
```

## 🎯 النتيجة المتوقعة

بعد التنفيذ، ستكون جميع التقارير:
- ✅ تعرض بيانات حقيقية من Firestore
- ✅ تتحدث تلقائياً عند تغيير البيانات
- ✅ تحسب الإحصائيات بدقة
- ✅ تعرض رسائل واضحة في حالة عدم وجود بيانات
- ✅ تحافظ على الأداء الجيد

## ⏱️ الوقت المتوقع
- إنشاء الخدمات: 2-3 ساعات
- تحديث التقارير: 2-3 ساعات
- الاختبار والتحسين: 1-2 ساعة
- **الإجمالي: 5-8 ساعات**

---

**هل تريد البدء في التنفيذ الآن؟** 🚀
