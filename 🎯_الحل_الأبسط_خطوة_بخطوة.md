# 🎯 الحل الأبسط - خطوة بخطوة

## الخطوة 1: تعيين المشروع في Cloud Shell

في Cloud Shell، نفذ:
```bash
gcloud config set project etisak-784d6
```

---

## الخطوة 2: رفع مجلد backend_v2

### الطريقة الأسهل: استخدام Cloud Shell Upload

1. **في Cloud Shell، اضغط على أيقونة "⋮" (ثلاث نقاط) في الأعلى**

2. **اختر "Upload folder"**

3. **اختر مجلد `backend_v2` من جهازك:**
   ```
   C:\Users\asus\my\almadrasah\backend_v2
   ```

4. **انتظر حتى يكتمل الرفع**

---

## الخطوة 3: النشر

بعد اكتمال الرفع، نفذ في Cloud Shell:

```bash
cd ~/backend_v2
gcloud run deploy schedule-solver \
  --source . \
  --region us-central1 \
  --allow-unauthenticated \
  --timeout 300 \
  --memory 2Gi \
  --cpu 2
```

---

## الخطوة 4: التحقق

```bash
curl https://schedule-solver-979291699789.us-central1.run.app/api/v2/health
```

يجب أن تحصل على:
```json
{
  "status": "healthy",
  "name": "School Schedule Solver V2",
  "version": "2.0.0"
}
```

---

## الخطوة 5: اختبار التطبيق

افتح: https://etisak-784d6.web.app

جرب توليد جدول - يجب أن يعمل الآن!

---

## إذا لم تجد "Upload folder"

استخدم هذه الطريقة البديلة:

### 1. ضغط المجلد في Windows
- اذهب إلى: `C:\Users\asus\my\almadrasah`
- انقر بزر الماوس الأيمن على `backend_v2`
- اختر "Send to" → "Compressed (zipped) folder"

### 2. رفع الملف المضغوط
- في Cloud Shell، اضغط "⋮" → "Upload"
- ارفع `backend_v2.zip`

### 3. فك الضغط
```bash
unzip backend_v2.zip
cd backend_v2
```

### 4. النشر (نفس الأمر أعلاه)
```bash
gcloud run deploy schedule-solver \
  --source . \
  --region us-central1 \
  --allow-unauthenticated \
  --timeout 300 \
  --memory 2Gi \
  --cpu 2
```

---

## ملاحظات مهمة

- ✅ التحديث الوحيد في ملف `precheck_service.py`
- ✅ بعد النشر، Flutter سيعمل تلقائياً
- ✅ لا حاجة لأي تحديثات إضافية
- ✅ وقت النشر: 2-3 دقائق

---

## إذا واجهت أي مشكلة

أرسل لي:
1. الأمر الذي نفذته
2. رسالة الخطأ كاملة
3. نتيجة: `ls -la ~/backend_v2`
