# 🔄 تحديث التقارير للبيانات الحقيقية

## ✅ ما تم إنجازه حتى الآن

### 1. Models (تم ✅)
- ✅ `learning_gap.dart` - نموذج الفجوات التعليمية
- ✅ `performance_stats.dart` - نماذج الإحصائيات

### 2. Services (تم ✅)
- ✅ `success_rates_service.dart` - خدمة حسابات نسب النجاح
- ✅ `learning_gaps_service.dart` - خدمة حسابات الفجوات
- ✅ `teacher_performance_service.dart` - خدمة حسابات أداء المعلمين

### 3. Providers (تم ✅)
- ✅ `academic_providers.dart` - Providers لجميع الخدمات

## 📝 التغييرات المطلوبة في التقارير

### تقرير 1: نسب النجاح
**الملف:** `lib/src/features/academic/presentation/success_rates_report_screen.dart`

**التغييرات:**

1. **إضافة import للـ Provider:**
```dart
import '../providers/academic_providers.dart';
import '../domain/performance_stats.dart';
```

2. **استبدال الأرقام الثابتة (السطر 467-470):**
```dart
// القديم:
final successRate = 87;
final excellenceRate = 45;
final improvementRate = 12;

// الجديد:
final performanceStatsAsync = ref.watch(performanceStatsProvider);
```

3. **تحديث build method لاستخدام AsyncValue:**
```dart
@override
Widget build(BuildContext context) {
  final user = ref.watch(authStateProvider).value;
  final schoolId = user?.schoolId ?? '';
  
  final studentsAsync = ref.watch(studentsProvider);
  final classesAsync = ref.watch(classesProvider);
  final performanceStatsAsync = ref.watch(performanceStatsProvider);
  
  return performanceStatsAsync.when(
    data: (stats) => _buildContent(context, stats, studentsAsync, classesAsync),
    loading: () => Scaffold(
      appBar: AppBar(...),
      body: Center(child: CircularProgressIndicator()),
    ),
    error: (error, stack) => Scaffold(
      appBar: AppBar(...),
      body: Center(child: Text('خطأ في تحميل البيانات')),
    ),
  );
}
```

4. **تحديث _buildContent لاستخدام البيانات الحقيقية:**
```dart
Widget _buildContent(
  BuildContext context,
  PerformanceStats stats,
  AsyncValue<List<User>> studentsAsync,
  AsyncValue<List<Classroom>> classesAsync,
) {
  final studentsCount = studentsAsync.value?.length ?? 0;
  final classesCount = classesAsync.value?.length ?? 0;
  
  return Scaffold(
    // استخدام stats.overallSuccessRate بدلاً من 87
    // استخدام stats.excellenceRate بدلاً من 45
    // استخدام stats.improvementRate بدلاً من 12
  );
}
```

5. **تحديث الرسم البياني الدائري:**
```dart
PieChart(
  PieChartData(
    sections: [
      PieChartSectionData(
        value: stats.performanceLevels['excellent'] ?? 0,
        title: 'ممتاز',
        color: Colors.green.shade600,
      ),
      PieChartSectionData(
        value: stats.performanceLevels['veryGood'] ?? 0,
        title: 'جيد جداً',
        color: Colors.blue.shade600,
      ),
      // ... باقي المستويات
    ],
  ),
),
```

6. **تحديث نسب النجاح حسب الفصول:**
```dart
// استخدام stats.successRatesByClass
stats.successRatesByClass.entries.map((entry) {
  return _buildClassCard(entry.key, entry.value);
}).toList()
```

7. **تحديث دالة _generatePDF:**
```dart
Future<void> _generatePDF(
  BuildContext context,
  PerformanceStats stats,
  int studentsCount,
  int classesCount,
) async {
  // استخدام stats بدلاً من الأرقام الثابتة
}
```

### تقرير 2: الفجوات التعليمية
**الملف:** `lib/src/features/academic/presentation/learning_gaps_report_screen.dart`

**التغييرات:**

1. **إضافة import:**
```dart
import '../providers/academic_providers.dart';
import '../domain/performance_stats.dart';
```

2. **استبدال الأرقام الثابتة:**
```dart
// القديم:
final gapsCount = 12;
final criticalGaps = 3;
final resolvedGaps = 8;

// الجديد:
final gapStatsAsync = ref.watch(gapStatsProvider);
```

3. **تحديث build method:**
```dart
return gapStatsAsync.when(
  data: (gapStats) => _buildContent(context, gapStats),
  loading: () => Scaffold(...),
  error: (error, stack) => Scaffold(...),
);
```

4. **تحديث قائمة الفجوات:**
```dart
ListView.builder(
  itemCount: gapStats.gaps.length,
  itemBuilder: (context, index) {
    final gap = gapStats.gaps[index];
    return _buildGapCard(
      '${gap.subject} - ${gap.className}',
      gap.description,
      gap.affectedStudents,
      gap.priority,
      _getPriorityColor(gap.priority),
      _getSubjectIcon(gap.subject),
    );
  },
)
```

### تقرير 3: أداء المعلمين
**الملف:** `lib/src/features/academic/presentation/teacher_performance_report_tab.dart`

**التغييرات:**

1. **إضافة import:**
```dart
import '../providers/academic_providers.dart';
import '../domain/performance_stats.dart';
```

2. **استبدال _loadData:**
```dart
// حذف دالة _loadData القديمة
// استخدام teacherStatsProvider بدلاً منها
```

3. **تحديث build method:**
```dart
final teacherStatsAsync = ref.watch(teacherStatsProvider);

return teacherStatsAsync.when(
  data: (stats) => _buildContent(context, stats),
  loading: () => Scaffold(...),
  error: (error, stack) => Scaffold(...),
);
```

4. **تحديث قائمة المعلمين:**
```dart
ListView.builder(
  itemCount: stats.length,
  itemBuilder: (context, index) {
    final teacherStat = stats[index];
    return _buildTeacherCard(teacherStat);
  },
)
```

## 🎯 الخطوات التالية

1. ✅ تم إنشاء جميع الخدمات والـ Models
2. ⏳ تحديث تقرير نسب النجاح
3. ⏳ تحديث تقرير الفجوات التعليمية
4. ⏳ تحديث تقرير أداء المعلمين
5. ⏳ اختبار جميع التقارير
6. ⏳ رفع التحديثات على Firebase

## ⚠️ ملاحظات مهمة

1. **الأداء**: الحسابات قد تأخذ وقتاً مع عدد كبير من الطلاب
2. **التخزين المؤقت**: يمكن إضافة caching لاحقاً لتحسين الأداء
3. **البيانات الناقصة**: التعامل مع الحالات التي لا توجد فيها بيانات كافية
4. **الأخطاء**: عرض رسائل خطأ واضحة للمستخدم

## 📊 مثال على البيانات

### قبل التحديث:
```dart
final successRate = 87;  // رقم ثابت
final excellenceRate = 45;  // رقم ثابت
```

### بعد التحديث:
```dart
final stats = await successRatesService.calculatePerformanceStats(schoolId);
// stats.overallSuccessRate = 87.5 (محسوب من البيانات الفعلية)
// stats.excellenceRate = 42.3 (محسوب من البيانات الفعلية)
```

---

**الحالة الحالية:** تم إنشاء البنية التحتية الكاملة ✅
**الخطوة التالية:** تحديث التقارير لاستخدام البيانات الحقيقية ⏳
