# 🎯 كيفية الوصول للأقسام الجديدة

## ✅ تم إنشاء لوحة تحكم موحدة!

تم إنشاء شاشة جديدة تجمع جميع الأقسام:
`lib/src/features/schedule/presentation/educational_affairs_dashboard.dart`

---

## 🚀 طريقة الاستخدام

### الطريقة 1: الوصول المباشر (للاختبار)

أضف هذا الكود في أي مكان في التطبيق للوصول المباشر:

```dart
import 'package:flutter/material.dart';
import 'lib/src/features/schedule/presentation/educational_affairs_dashboard.dart';

// في أي مكان تريد الوصول للوحة
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (context) => EducationalAffairsDashboard(
      schoolId: 'your_school_id', // ضع معرف المدرسة هنا
    ),
  ),
);
```

### الطريقة 2: إضافة زر في الشاشة الرئيسية

إذا كان لديك شاشة رئيسية للمدير أو وكيل الشؤون، أضف زر:

```dart
ElevatedButton(
  onPressed: () {
    Navigator.push(
      context,
      MaterialPageRoute(
        builder: (context) => EducationalAffairsDashboard(
          schoolId: currentUser.schoolId,
        ),
      ),
    );
  },
  child: const Text('وكيل الشؤون التعليمية'),
)
```

### الطريقة 3: إضافة في القائمة الجانبية (Drawer)

```dart
Drawer(
  child: ListView(
    children: [
      // ... عناصر أخرى
      ListTile(
        leading: const Icon(Icons.school),
        title: const Text('وكيل الشؤون التعليمية'),
        onTap: () {
          Navigator.pop(context); // إغلاق الـ Drawer
          Navigator.push(
            context,
            MaterialPageRoute(
              builder: (context) => EducationalAffairsDashboard(
                schoolId: currentUser.schoolId,
              ),
            ),
          );
        },
      ),
    ],
  ),
)
```

### الطريقة 4: استخدام GoRouter (إذا كنت تستخدمه)

أضف المسار في `GoRouter`:

```dart
GoRoute(
  path: '/educational-affairs',
  builder: (context, state) {
    final schoolId = state.queryParams['schoolId'] ?? '';
    return EducationalAffairsDashboard(schoolId: schoolId);
  },
),
```

ثم استخدمه:

```dart
context.go('/educational-affairs?schoolId=$schoolId');
```

---

## 📱 ما ستراه في اللوحة

عند فتح `EducationalAffairsDashboard`، ستظهر 4 بطاقات:

### 1. الجدول الذكي 🤖 (بنفسجي)
- الشاشة الموجودة مسبقاً
- توليد جدول ذكي تلقائياً

### 2. الجدول التشاركي 📋 (أزرق)
- **جديد!**
- إنشاء حملة لجمع رغبات المعلمين
- لوحة تحكم بإحصائيات حية
- شاشة للمعلمين لاختيار الحصص المحظورة

### 3. توزيع النصاب ⚖️ (أخضر)
- **جديد!**
- تحليل نصاب المعلمين
- مؤشر العدالة (0-100%)
- توصيات ذكية
- نظام تعلم من البيانات التاريخية

### 4. سجل التعديلات 📝 (برتقالي)
- **جديد!**
- تتبع جميع التعديلات
- خط زمني تفاعلي
- إحصائيات وتحليلات
- مقارنة بين الجداول

---

## 🧪 اختبار سريع

### 1. اختبار اللوحة الرئيسية

```dart
// في main.dart أو أي ملف اختبار
void main() {
  runApp(MaterialApp(
    home: EducationalAffairsDashboard(
      schoolId: 'test_school_123',
    ),
  ));
}
```

### 2. اختبار قسم معين

```dart
// اختبار الجدول التشاركي
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (context) => CreateCampaignScreen(
      schoolId: 'test_school_123',
    ),
  ),
);

// اختبار توزيع النصاب
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (context) => WorkloadAnalysisScreen(
      schoolId: 'test_school_123',
    ),
  ),
);

// اختبار سجل التعديلات
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (context) => ModificationsLogScreen(
      schoolId: 'test_school_123',
    ),
  ),
);
```

---

## 🔐 الصلاحيات

تأكد من أن المستخدم لديه صلاحية "وكيل الشؤون التعليمية":

```dart
// مثال: التحقق من الصلاحية قبل الوصول
if (currentUser.role == 'educational_affairs_deputy' || 
    currentUser.role == 'principal') {
  Navigator.push(
    context,
    MaterialPageRoute(
      builder: (context) => EducationalAffairsDashboard(
        schoolId: currentUser.schoolId,
      ),
    ),
  );
} else {
  ScaffoldMessenger.of(context).showSnackBar(
    const SnackBar(content: Text('ليس لديك صلاحية الوصول')),
  );
}
```

---

## 📊 البيانات المطلوبة

تأكد من وجود:
1. ✅ `schoolId` - معرف المدرسة
2. ✅ معلمون في `Schools/{schoolId}/Teachers`
3. ✅ جدول دراسي في `Schools/{schoolId}/Schedules`

---

## 🎨 التخصيص

يمكنك تخصيص الألوان والأيقونات في `educational_affairs_dashboard.dart`:

```dart
_buildSectionCard(
  context,
  title: 'عنوان مخصص',
  icon: Icons.custom_icon,
  color: Colors.customColor,
  description: 'وصف مخصص',
  onTap: () => // الإجراء
),
```

---

## ✅ الخلاصة

1. ✅ تم إنشاء لوحة تحكم موحدة
2. ✅ تجمع الجدول الذكي + 3 أقسام جديدة
3. ✅ جاهزة للاستخدام الفوري
4. ✅ 0 أخطاء في الكود

**افتح `EducationalAffairsDashboard` وستظهر جميع الأقسام! 🎉**
