# خطوات التحقق وإثبات الحل (Verification Steps)

عزيزي المستخدم،
بما أنني مساعد ذكي (AI) يعمل في بيئة تطوير، لا يمكنني الوصول المباشر لالتقاط صور من متصفحك أو من وحدة تحكم Firebase الخاصة بك.
لذلك، قمت بتجهيز هذا الدليل لتمكينك من التحقق بنفسك والتقاط الصور المطلوبة لإغلاق هذه المشاكل (P0 Bugs).

## 1. إثبات حل مشكلة تغيير كلمة المرور (Bug 1 - Password Change Loop)

**المطلوب:** لقطة شاشة من Firestore Console توضح أن وثيقة المستخدم تحتوي على `isPasswordChangeRequired=false` و `passwordChangedAt`.

**خطوات التحقق:**
1. افتح مشروعك في Firebase Console: [https://console.firebase.google.com/](https://console.firebase.google.com/)
2. انتقل إلى **Firestore Database** -> **Data**.
3. ابحث عن مجموعة `GlobalUsers`.
4. اختر وثيقة المستخدم الذي قمت بتغيير كلمة مروره للتو.
5. **التقط صورة** توضح الحقول التالية:
   - `isPasswordChangeRequired`: `false`
   - `passwordChangedAt`: (Timestamp)

**كيفية التجربة في التطبيق:**
1. قم بتشغيل التطبيق: `flutter run -d chrome --web-port 5000`
2. سجل الدخول بحساب جديد.
3. سيطلب منك تغيير كلمة المرور. قم بتغييرها.
4. حاول تحديث الصفحة أو تسجيل الدخول من متصفح آخر. لن يطلب منك التغيير مرة أخرى.

---

## 2. إثبات حل مشكلة فهارس لوحة الذكاء (Bug 2 - Firestore Indexes)

**المطلوب:** لقطة شاشة للفهارس وهي بحالة `Enabled` ولقطة للوحة الذكاء وهي تعمل.

**خطوات التحقق من الفهارس:**
1. انتقل إلى **Firestore Database** -> **Indexes** في Firebase Console.
2. انقر على تبويب **Composite Indexes**.
3. ابحث عن الفهارس الخاصة بـ `behavior_records`.
4. **التقط صورة** توضح أن الحالة هي **Enabled** (باللون الأخضر).

**خطوات التحقق من اللوحة:**
1. في التطبيق، انتقل إلى "لوحة ذكاء منار" (Intelligence Dashboard).
2. تأكد من أن البيانات تظهر (حتى لو كانت أصفاراً) ولا توجد رسالة خطأ باللون الأحمر.
3. **التقط صورة** للوحة وهي تعمل بشكل طبيعي.

---

## ملاحظات إضافية
- تم نشر الفهارس بالفعل باستخدام الأمر `firebase deploy --only firestore:indexes`.
- تم تعديل الكود ليعتمد كلياً على حالة السيرفر (Server-Side State) في `GlobalUsers`، مما يمنع تكرار طلب تغيير كلمة المرور.
