# 🔧 حل مشكلة "فشل الفحص الأولي"

## المشكلة
الرسالة: "فشل الفحص الأولي. أصلح البيانات ثم أعد المحاولة"

## السبب
Backend V2 كان يتحقق من أن مجموع الحصص الأسبوعية = 35 حصة بالضبط (5 أيام × 7 حصص)
لكن البيانات المرسلة قد تكون أقل من ذلك.

## الحل (تم تطبيقه)
تم تعديل Backend V2 ليكون أكثر مرونة:
- ✅ يقبل أي عدد حصص أقل من 35
- ✅ يرفض فقط إذا تجاوز 35 حصة
- ✅ يعطي تحذير فقط إذا كان أقل من 35

## خطوات النشر

### الطريقة 1: من Cloud Shell (الأسهل)
1. افتح Cloud Shell: https://console.cloud.google.com/cloudshell
2. نفذ:
```bash
cd ~/almadrasah
git pull  # إذا كنت تستخدم Git

# أو ارفع ملف precheck_service.py المحدث يدوياً
cd backend_v2
gcloud run deploy schedule-solver \
  --source . \
  --region us-central1 \
  --allow-unauthenticated \
  --timeout 300 \
  --memory 2Gi \
  --cpu 2
```

### الطريقة 2: رفع الملف المحدث فقط
1. افتح Cloud Shell Editor: https://console.cloud.google.com/cloudshell/editor
2. اذهب إلى: `backend_v2/app/services/precheck_service.py`
3. استبدل المحتوى بالملف المحدث من مشروعك المحلي
4. نفذ الأمر أعلاه

### الطريقة 3: من Windows (إذا كان gcloud مثبت)
```powershell
cd C:\Users\asus\my\almadrasah
cd backend_v2
gcloud run deploy schedule-solver --source . --region us-central1 --allow-unauthenticated --timeout 300 --memory 2Gi --cpu 2
```

## التحقق من النشر
بعد النشر، اختبر:
```bash
curl https://schedule-solver-979291699789.us-central1.run.app/api/v2/health
```

يجب أن تحصل على:
```json
{
  "status": "healthy",
  "name": "School Schedule Solver V2",
  "version": "2.0.0"
}
```

## اختبار التطبيق
1. افتح: https://etisak-784d6.web.app
2. سجل دخول
3. اذهب لشاشة توليد الجدول
4. اضغط "توليد الجدول"
5. يجب أن يعمل الآن بدون مشاكل

## ملاحظات
- لا حاجة لتحديث Flutter - التحديث في Backend فقط
- بعد النشر، التطبيق سيعمل تلقائياً
- إذا استمرت المشكلة، تحقق من:
  - عدد الحصص المطلوبة لكل فصل
  - وجود معلمين وفصول وإسنادات

## الملف المحدث
`backend_v2/app/services/precheck_service.py` - تم تعديل التحقق من سعة الفصل

---

**الحالة**: ✅ الإصلاح جاهز - يحتاج نشر فقط
