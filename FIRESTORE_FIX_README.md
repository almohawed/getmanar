# 🔧 دليل الإصلاح التلقائي لقواعد Firestore

## 🎯 الهدف

إصلاح مشكلة صلاحيات Firestore بشكل **تلقائي كامل** بدون أي تدخل يدوي.

---

## ⚡ الطريقة السريعة (Windows)

### 1. احصل على Service Account Key
- افتح: https://console.firebase.google.com
- اختر مشروع: **etisak-784d6**
- اذهب إلى: **Project Settings > Service Accounts**
- اضغط: **Generate new private key**
- احفظ الملف باسم: `serviceAccountKey.json`
- ضعه في المجلد الرئيسي للمشروع

### 2. شغّل الملف
```bash
# اضغط دبل كليك على:
fix_firestore.bat
```

### 3. انتظر النتيجة
- سيقوم السكريبت بكل شيء تلقائياً
- سترى رسالة: "✅ تم الإصلاح بنجاح!"

---

## 🖥️ الطريقة اليدوية (جميع الأنظمة)

### 1. تثبيت المكتبات
```bash
npm install google-auth-library
```

### 2. تشغيل السكريبت
```bash
node auto_fix_firestore.js
```

---

## 📋 ما يقوم به السكريبت

### 1. التحقق من الملفات
- ✅ يتحقق من وجود `serviceAccountKey.json`
- ✅ يقرأ معلومات المشروع

### 2. الاتصال بـ Firebase
- ✅ يحصل على Access Token
- ✅ يتصل بـ Firebase Admin API

### 3. تحديث القواعد
- ✅ يرسل القواعد الجديدة
- ✅ ينشرها تلقائياً

### 4. حفظ نسخة احتياطية
- ✅ يحفظ القواعد في `firestore_rules_backup.txt`
- ✅ يمكن استخدامها للنسخ اليدوي

---

## 🔒 القواعد الجديدة

### المميزات:
1. **آمنة**: كل مدرسة ترى بياناتها فقط
2. **سريعة**: بدون تعقيدات أو استعلامات إضافية
3. **مفتوحة للقراءة**: السماح بقراءة البيانات للمستخدمين المصرح لهم
4. **محمية للكتابة**: الكتابة محمية حسب الدور

### الأمان:
- ✅ كل مستخدم يرى فقط بيانات مدرسته
- ✅ لا يمكن الوصول لبيانات مدارس أخرى
- ✅ الكتابة محمية حسب الدور (admin, teacher, deputy)
- ✅ جميع الاستعلامات محمية بـ schoolId

---

## 🧪 الاختبار

بعد تشغيل السكريبت:

### 1. افتح التطبيق
```
https://etisak-784d6.web.app
```

### 2. سجل الدخول
- استخدم حساب وكيل شؤون طلاب

### 3. تحقق من:
- ✅ لا أخطاء في Console
- ✅ الأرقام الحقيقية تظهر (ليست 450)
- ✅ إحصائيات الحضور والغياب صحيحة
- ✅ السرعة ممتازة

---

## 🔧 الحل البديل (يدوي)

إذا فشل السكريبت:

### 1. افتح Firebase Console
```
https://console.firebase.google.com/project/etisak-784d6/firestore/rules
```

### 2. انسخ القواعد
- افتح ملف: `firestore_rules_backup.txt`
- انسخ المحتوى كاملاً

### 3. الصق في محرر القواعد
- الصق القواعد في محرر Firebase

### 4. انشر
- اضغط زر **Publish**

**الوقت المطلوب**: دقيقتان فقط!

---

## ⚠️ ملاحظات مهمة

### 1. Service Account Key
- **لا تشاركه مع أحد أبداً**
- **لا ترفعه على Git** (محمي تلقائياً في `.gitignore`)
- **احفظه في مكان آمن**
- يعطي صلاحيات كاملة على المشروع

### 2. القواعد
- تطبق فوراً بعد النشر
- لا حاجة لإعادة تشغيل التطبيق
- تعمل مع جميع المستخدمين

### 3. النسخة الاحتياطية
- يتم حفظها تلقائياً في `firestore_rules_backup.txt`
- يمكنك استخدامها للنسخ اليدوي
- احتفظ بها للمستقبل

---

## 🐛 حل المشاكل

### المشكلة: "لم يتم العثور على serviceAccountKey.json"
**الحل**: 
1. تأكد من وضع الملف في المجلد الرئيسي
2. تأكد من اسم الملف: `serviceAccountKey.json` (بالضبط)

### المشكلة: "فشل الحصول على Access Token"
**الحل**:
1. تأكد من صحة ملف `serviceAccountKey.json`
2. تأكد من اتصال الإنترنت
3. جرب تنزيل مفتاح جديد

### المشكلة: "فشل تحديث القواعد"
**الحل**:
1. استخدم الحل اليدوي (دقيقتان)
2. انسخ من `firestore_rules_backup.txt`
3. الصق في Firebase Console

---

## 📊 بنية القواعد

### المجموعات المحمية:
- `Users` - قراءة للمستخدم نفسه أو من نفس المدرسة
- `Schools` - قراءة لأي مستخدم من نفس المدرسة
- `StudentAttendance` - قراءة وكتابة لنفس المدرسة
- `behavior_records` - قراءة وكتابة لنفس المدرسة
- `students` - قراءة وكتابة لنفس المدرسة

### الدوال المساعدة:
```javascript
function getUserSchoolId() {
  return get(/databases/$(database)/documents/Users/$(request.auth.uid)).data.schoolId;
}

function isSameSchool(schoolId) {
  return request.auth != null && getUserSchoolId() == schoolId;
}
```

---

## 📁 الملفات المتضمنة

| الملف | الوصف |
|------|-------|
| `auto_fix_firestore.js` | السكريبت الرئيسي للإصلاح التلقائي |
| `fix_firestore.bat` | ملف Windows Batch للتشغيل السريع |
| `package_firestore_fix.json` | ملف package.json للمكتبات |
| `⚡_ابدأ_هنا.txt` | تعليمات سريعة بالعربية |
| `🤖_الإصلاح_التلقائي_الكامل.md` | دليل شامل بالعربية |
| `FIRESTORE_FIX_README.md` | هذا الملف |

---

## 🚀 الخلاصة

**3 خطوات فقط:**
1. ضع `serviceAccountKey.json` في المشروع
2. شغّل `fix_firestore.bat` (Windows) أو `node auto_fix_firestore.js`
3. ✅ تم الإصلاح!

**لا حاجة لأي تدخل يدوي!**

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من وجود `serviceAccountKey.json`
2. تحقق من اتصال الإنترنت
3. استخدم الحل اليدوي (دقيقتان)
4. راجع قسم "حل المشاكل" أعلاه

---

**تاريخ الإنشاء**: 2026-04-16  
**الإصدار**: 3.0.0  
**الحالة**: ✅ جاهز للاستخدام  
**المطور**: Kiro AI

