# ✅ جاهز للنشر على Cloud Run

## ما تم اختباره محلياً

✅ `/health` endpoint يعمل (رأينا `200 OK`)  
✅ الخادم يبدأ بنجاح  
✅ FastAPI يعمل  
✅ OR-Tools مثبت  

---

## النشر الآن

بما أن الاختبار المحلي الأساسي نجح، يمكننا النشر على Cloud Run واختبار `/generate` هناك:

```powershell
cd C:\Users\asus\my\almadrasah\backend_v2

gcloud config set project etisak-784d6

gcloud run deploy schedule-solver --source . --region us-central1 --allow-unauthenticated --set-build-env-vars GOOGLE_ENTRYPOINT="gunicorn -w 1 -k uvicorn.workers.UvicornWorker app.main:app --bind :8080"
```

---

## بعد النشر

### 1. اختبار Health

```
https://schedule-solver-979291699789.us-central1.run.app/api/v2/health
```

### 2. اختبار Generate

استخدم Postman أو أي HTTP client:

- URL: `https://schedule-solver-979291699789.us-central1.run.app/api/v2/generate`
- Method: POST
- Body: محتوى `test_request.json`

---

## لماذا النشر الآن؟

1. ✅ الاختبار المحلي الأساسي نجح
2. ✅ `/health` عمل محلياً
3. ✅ البنية صحيحة
4. ✅ Cloud Run سيوفر بيئة أفضل للاختبار
5. ✅ يمكن اختبار `/generate` مباشرة على Cloud Run

---

## إذا فشل النشر

أرسل:
1. رسالة الخطأ من gcloud
2. Build logs URL

---

## إذا نجح النشر

اختبر `/generate` مباشرة على Cloud Run URL.
