# خطوات التحقق والإثبات (Verification Steps)

لقد تم تنفيذ جميع التعديلات المطلوبة. بما أنني مساعد ذكي ولا أملك الوصول المادي لجهازك أو لوحة تحكم Firebase الخاصة بك، يرجى اتباع الخطوات التالية لجمع الإثباتات المطلوبة (Proofs) التي طلبتها.

## 1. P0 - التوجيه الصارم حسب الصلاحية (Strict Role-Based Routing)
**الهدف**: التأكد من أن النظام يعتمد *فقط* على `GlobalUsers` كمصدر للحقيقة.

*   **الإجراء**: قم بتسجيل الدخول بحساب "مدير مدرسة" (Manager) الذي كان يواجه المشكلة.
*   **المتوقع**: يتم توجيهك مباشرة إلى **AdminDashboard**.
*   **الإثباتات المطلوبة منك**:
    1.  **Firebase Console**: خذ لقطة شاشة لملف المستخدم في `GlobalUsers/{uid}` وتأكد من وجود الحقل: `role: "admin"`.
    2.  **فيديو**: سجل فيديو لعملية تسجيل الدخول يظهر الانتقال المباشر للوحة المدير.
    3.  **Logs**: ابحث في `debug console` (أو Logcat) عن السطر التالي عند الدخول:
        ```text
        AUTH_RESOLVED_FROM_GLOBALUSERS → role=admin
        ```

## 2. P0 - تغيير كلمة المرور (Android)
**الهدف**: التأكد من عدم ظهور خطأ `permission-denied`.

*   **الإجراء**: من تطبيق الأندرويد، اطلب تغيير كلمة المرور (أو سجل دخول لأول مرة كمدير).
*   **المتوقع**: يتم التغيير بنجاح وتحديث `GlobalUsers` في السيرفر عبر Cloud Function `completePasswordChange`.
*   **الإثباتات المطلوبة منك**:
    1.  **فيديو**: يوضح العملية كاملة بدون أخطاء.

## 3. P1 - إعدادات الرسائل (SMS Settings)
**الهدف**: إزالة الرابط الافتراضي وتعديل النص التلميحي.

*   **الإجراء**: اذهب إلى شاشة إعدادات الرسائل.
*   **المتوقع**:
    *   حقل الرابط فارغ تمامًا (لا يوجد `https://app.mobile.net.sa...`).
    *   النص التلميحي (Hint) هو: "ضع رابط الرسائل الخاص بك هنا".
*   **الإثباتات المطلوبة منك**:
    1.  **لقطة شاشة**: لشاشة الإعدادات تظهر التعديلات.

## 4. P1 - أيقونة التطبيق (App Icon)
**الهدف**: تغيير أيقونة Flutter الافتراضية إلى أيقونة "منار".

*   **الإجراء**: ثبت ملف APK الجديد.
*   **المتوقع**: ظهور شعار "منار" على شاشة التطبيقات الرئيسية.
*   **الإثباتات المطلوبة منك**:
    1.  **لقطة شاشة**: لشاشة الجوال تظهر الأيقونة الجديدة.

---

## ملفات التثبيت (Artifacts)
*   **Android APK**: `build/app/outputs/flutter-apk/app-release.apk`
*   **Web**: `build/web/` (أو عبر الرابط المحلي المعروض)
