# الحل الجذري الفوري - Invalid dayIndex Fix

## المشكلة الأساسية
```
⚠️ Invalid dayIndex: null for lesson: {day: الاثنين, ...}
📊 Total lessons in schedule: 0
```

البيانات موجودة لكن `dayIndex` فارغ = الجداول لا تظهر

## الحل الفوري (3 خطوات)

### 1️⃣ أضف هذا الملف الجديد:
**المسار:** `lib/src/features/schedule/services/day_index_mapper.dart`

```dart
class DayIndexMapper {
  static const Map<String, int> arabicDayMap = {
    'الأحد': 0,
    'الاثنين': 1,
    'الثلاثاء': 2,
    'الأربعاء': 3,
    'الخميس': 4,
  };

  static int getDayIndex(String? dayName) {
    if (dayName == null || dayName.isEmpty) return -1;
    return arabicDayMap[dayName] ?? -1;
  }

  static String getDayName(int dayIndex) {
    const days = ['الأحد', 'الاثنين', 'الثلاثاء', 'الأربعاء', 'الخميس'];
    if (dayIndex < 0 || dayIndex >= days.length) return '';
    return days[dayIndex];
  }

  static bool isValidDay(String? dayName) {
    return dayName != null && arabicDayMap.containsKey(dayName);
  }
}
```

### 2️⃣ عدّل ملف تحميل الجداول:
**المسار:** `lib/src/features/schedule/services/schedule_cache_manager.dart`

ابحث عن دالة تحميل الدروس وأضف هذا:

```dart
// قبل إرجاع الدروس
for (var lesson in lessons) {
  if (lesson['dayIndex'] == null && lesson['day'] != null) {
    lesson['dayIndex'] = DayIndexMapper.getDayIndex(lesson['day']);
  }
}
```

### 3️⃣ امسح الـ Cache:
في Firebase Console:
```
1. اذهب إلى Firestore
2. اختر collection: schedules
3. احذف جميع الوثائق
4. أعد تشغيل التطبيق
```

أو من الكود:
```dart
// في أي مكان في التطبيق
await FirebaseFirestore.instance
    .collection('schedules')
    .get()
    .then((snapshot) {
  for (var doc in snapshot.docs) {
    doc.reference.delete();
  }
});
```

## التحقق من النجاح:
- ✅ لا تظهر رسائل `Invalid dayIndex: null`
- ✅ `Total lessons in schedule: > 0`
- ✅ الجداول تظهر بشكل صحيح

## إذا لم ينجح:
1. تأكد من أن Backend يرسل `dayName` صحيح
2. تحقق من أن `dayIndex` يُحسب قبل الحفظ في Firestore
3. امسح بيانات التطبيق المحلية: `flutter clean`
